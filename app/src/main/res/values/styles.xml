<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->

    </style>

    <style name="common_btn">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/common_btn</item>
        <item name="android:textSize">@dimen/font_14</item>
    </style>

    <style name="common_search_btn">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/com_btn_search</item>
        <item name="android:textSize">@dimen/font_14</item>
    </style>

    <style name="idle_tool_btn">
        <item name="android:background">@drawable/idle_tool_btn</item>
    </style>
</resources>
